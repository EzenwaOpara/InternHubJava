<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oparabenjamin.internhubjava">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ArraysEActivity"></activity>
        <activity android:name=".PolyEActivity" />
        <activity android:name=".ExceptEActivity" />
        <activity android:name=".InheritanceEActivity" />
        <activity android:name=".CtrlEActivity" />
        <activity android:name=".ClassObjEActivity" />
        <activity android:name=".JavaIOEActivity" />
        <activity android:name=".ArraysActivity" />
        <activity android:name=".PolyActivity" />
        <activity android:name=".ExceptActivity" />
        <activity android:name=".InheritanceActivity" />
        <activity android:name=".CtrlActivity" />
        <activity android:name=".ClassObjActivity" />
        <activity android:name=".JavaIOActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>